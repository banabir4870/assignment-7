import TimelineClient from "@/components/TimelineClient/TimelineClient";

export const metadata = {
  title: "KeenKeeper | Timeline",
  description: "",
};

const TimelinePage = () => {
    return (
        <TimelineClient></TimelineClient>
    );
};

export default TimelinePage;